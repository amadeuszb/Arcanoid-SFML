#include "Game.h"

int main()
{
	
	Game Start(800,600);
	Start.PlayGame();
	
	return 0;
}
/*We have to make a class which will be main class for ball and paddle beacuse they have similar methods and code will be much easier and clean*/

/*We can to make a joined class for the ball and the paddle, as they utilise similar methods. This will result in a much cleaner code that is easier to read*/